# External References

실제 외부 공개 저장소는 프로젝트 루트의 `_references/` 폴더에 clone한다.
`_references/`는 `.gitignore` 대상이며 이 프로젝트 저장소에 커밋하지 않는다.

사용 방법은 `OPEN_SOURCE_REFERENCE_GUIDE.md`와 `scripts/fetch_references.*`를 참고한다.

주의: PM4Py/Apromore/Cortado 등 copyleft source는 opt-in으로만 내려받으며 제품 소스에 직접 복사하지 않는다.
